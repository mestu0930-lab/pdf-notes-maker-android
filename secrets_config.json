{
  "signing_secret": "\xe6\xc9\xc4!\xaf\xe5l\x98RY\xdf\xf9z\xa3\x1e\xef",
  "notes": {
    "description": "This file contains the signing secret used to HMAC-sign all requests to the license server. It provides basic authentication to prevent unauthorized license activation requests.",
    "warning": "KEEP THIS FILE SECURE - Do not commit to version control",
    "location": "Place this file in the app root directory (same location as launcher.py)",
    "generation": "The signing secret should be a cryptographically secure random value, typically generated once during app setup and kept consistent across all app instances."
  },
  "example_secret_generation": {
    "python_command": "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())",
    "instructions": [
      "1. Run the Python command above in a terminal",
      "2. Copy the output string",
      "3. Replace 'your_base64_encoded_signing_secret_here' with the copied string",
      "4. Save this file",
      "5. Make sure launcher.py can find this file at app startup"
    ]
  }
}
