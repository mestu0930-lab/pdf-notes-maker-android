# PDF Notes Maker - requirements.txt
# For LOCAL DEVELOPMENT only (pip install -r requirements.txt)
# These are NOT the same as buildozer.spec requirements.
# Packages like openai, pydantic, httpx are fine for desktop/dev but
# CANNOT be used in buildozer.spec (no p4a recipe = HTTP 404 build error).
 
# ========== Core Framework ==========
kivy==2.3.0
kivy-garden==0.1.5
 
# ========== Web/API Communication ==========
requests==2.31.0
urllib3==2.1.0
certifi==2023.7.22
charset-normalizer==3.3.2
idna==3.6
 
# NOTE: httpx and aiohttp work fine locally but have no p4a recipe.
# If your Android app uses them, you need a custom p4a recipe.
httpx==0.25.2
aiohttp==3.9.1
 
# ========== AI/LLM Integration ==========
# openai and pydantic work locally but have NO p4a recipe.
# They cannot be bundled via buildozer.spec for Android.
openai==1.3.9
pydantic==2.5.0
# pydantic-core is installed automatically by pydantic — do not pin separately
typing-extensions==4.8.0
 
# ========== Document Processing ==========
pypdf==3.17.1
python-docx==0.8.11
markdown==3.5.1
python-dateutil==2.8.2
 
# ========== Encryption & Security ==========
cryptography==41.0.7
cffi==1.16.0
pycparser==2.21
 
# ========== Async Support ==========
aiofiles==23.2.1
 
# ========== Android/Platform Specific ==========
# pyjnius and plyer are only relevant on Android; they can be installed
# locally but won't do anything useful on desktop.
pyjnius==1.4.1
plyer==2.1.0
 
# ========== Utilities ==========
six==1.16.0
